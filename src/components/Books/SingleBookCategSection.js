import React, { Fragment } from 'react'

const SingleNewBook = () => {
   
    return (
        <Fragment>
            {/* <div className="first-list">
            {populars.map((popular)=>(
                <Link key={populars.id} to="/categories/books/worldbooks" className="single-popular">
                <div className="single-popular" >
                <img src={popular.image_src} alt=""/>
                <div className="popular-single-text dark-text"><h3>{popular.categ}</h3></div>
                </div>
                </Link>
            ))}
            </div> */}
        </Fragment>
    )
}

export default SingleNewBook
